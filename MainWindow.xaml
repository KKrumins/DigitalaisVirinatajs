<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Digitālais virinātājs" Height="380" Width="340"
        Topmost="True" ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,8">
                <TextBlock Text="Digitālais virinātājs" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock Text="🧥" FontSize="22" Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock Grid.Row="1" x:Name="TimerText" Text="00:00:00" FontSize="28"
                       FontFamily="Consolas" HorizontalAlignment="Center" Margin="0,0,0,4"/>

            <TextBlock Grid.Row="2" x:Name="StatusText" Text="Select apps and press Start"
                       FontSize="13" HorizontalAlignment="Center" Margin="0,0,0,8"
                       Foreground="Gray"/>

            <Border Grid.Row="3"
                    Margin="0,0,0,8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Ko virināt?" FontWeight="SemiBold"
                               Margin="8,6,8,4"/>
                    <ListBox Grid.Row="1" x:Name="AppListBox" SelectionMode="Multiple"
                             BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding ProcessName}"
                                          IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                          Margin="2"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <Button Grid.Row="2" Content="↻ Refresh" Click="RefreshButton_Click"
                            HorizontalAlignment="Right" Margin="8,4" Padding="10,3"
                            FontSize="11"/>
                </Grid>
            </Border>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="StartButton" Content="▶ Start" Click="StartButton_Click"
                        Padding="24,8" Margin="0,0,8,0" FontSize="14"/>
                <Button x:Name="StopButton" Content="⏹ Stop" Click="StopButton_Click"
                        Padding="24,8" FontSize="14" IsEnabled="False"/>
            </StackPanel>
        </Grid>
</Window>
